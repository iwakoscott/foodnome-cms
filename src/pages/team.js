import React from 'react';
import Layout from '../components/layout';

export default props => (
  <Layout {...props}>
    <div style={{ minHeight: '100vh', margin: '56px 0' }}>
      <h1>Hello, world!</h1>
    </div>
  </Layout>
);
